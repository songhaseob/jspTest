8일차 2021.01.06
   
쿠키 확인 방법
document.cookie

** 테스트 메소드 명명 규칙 : 테스트할 메소드 + "Test"
   .테스트 코드는 답을 모르면 테스트 하기 힘들다고 본다
   
쿠키를 이용하여 구현하고자 하는 기능

rememberme를 체크하고 로그인을 했을 때 userid와 rememberme 라는 이름으로 쿠키를 저장하여
다시 login.jsp에 접속 했을 때 사용자 id가 마지막 접속 시 아이디로 값이 채워지고
rememberme 체크박스가 체크가 되어있도록 설정

$(function(){
	클릭이벤트 감지 (sign in 버튼 클릭)
	쿠키 설정
	submit;
});